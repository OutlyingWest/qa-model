# MCQ training configuration
task: mcq
data_file: train_dataset_mcq.csv
val_ratio: 0.15
epochs: 3
batch_size: 4
gradient_accumulation_steps: 4
learning_rate: 1e-4
warmup_ratio: 0.1
scheduler: cosine
max_seq_length: 512
logging_steps: 10
save_steps: 100
eval_steps: 100
save_total_limit: 2
fp16: true
bf16: false
